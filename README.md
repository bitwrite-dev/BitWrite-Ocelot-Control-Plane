# BitWrite-Ocelot-Control-Plane
BitWrite Ocelot Control is a **Gateway Configuration and Control Platform** for managing Ocelot Gateway configuration through a centralized management plane.  The platform is not intended to be a simple CRUD interface for `ocelot.json`. Its primary responsibility is to manage gateway configuration as a controlled lifecycle:
